
<div class = "today">
<h3>  Results </h3>
<% @concerts.each do |concert| %>
<div>
<%= link_to "#{concert.artist}", "/concerts/#{concert.id}" %> at <%= concert.venue %>
<br><%= sprintf('%.2f', concert.price) %>
</div>
<%end%>
</div>

   <%= render("shared/sidebar")%>   


